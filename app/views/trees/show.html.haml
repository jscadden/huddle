#tree_nodes
  = render "tree_nodes", :node => @tree.node

#nodes_wrapper
  .reply
    %a(href="#") Reply
  #node_contents

#nodes
  %ol= render "node", :node => @tree.node, :root => true

= content_for(:right_column) do
  #slider_wrapper
    %input#slider(type="text" value="1")
  %canvas#huddle_canvas(width="240" height="240")
