<% title "Nodes" %>

<table>
  <tr>
    <th>Title</th>
    <th>Description</th>
  </tr>
  <% for node in @nodes %>
    <tr>
      <td><%=h node.title %></td>
      <td><%=h node.description %></td>
      <td><%= link_to "Show", node %></td>
      <td><%= link_to "Edit", edit_node_path(node) %></td>
      <td><%= link_to "Destroy", node, :confirm => 'Are you sure?', :method => :delete %></td>
    </tr>
  <% end %>
</table>

<p><%= link_to "New Node", new_node_path %></p>
